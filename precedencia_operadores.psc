Algoritmo Precedencia_operadores
	//Prioridad de ejecucion de operadores
	//1ero.parentesis ()
	//2do. potencia 
	//3ro. multiplicacion division modulo
	//4to. suma y resta
	//5to. operadores relacionales < > 
	//6to. opesradores logicos y, O, !
	
	
	//ej se revisa de izquierda a derecha
	a = 12/3 + 3 * 2 - 1
	imprimir 'resultado a evaluar -> 12/3 + 3 * 2 - 1= ', a
	
	a = 12/ (3 + 3) * (2 - 1)
	imprimir 'resultado a evaluar -> 12/ (3 + 3) * (2 - 1)', a
FinAlgoritmo
